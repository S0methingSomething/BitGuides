{
  "name": "bitguides-docs",
  "version": "1.0.0",
  "description": "Linters for the BitGuides documentation.",
  "private": true,
  "scripts": {
    "lint": "prettier --check 'docs/**/*.md'"
  },
  "devDependencies": {
    "prettier": "*"
  }
}
